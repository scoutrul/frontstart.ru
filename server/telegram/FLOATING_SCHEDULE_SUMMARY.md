# Реализация плавающего расписания постов

## Статус: ✅ ЗАВЕРШЕНО

Дата: 2026-01-10

## Что изменилось

### Старая система
- ❌ 2 поста в день в фиксированное время (09:00 и 18:00 МСК)
- ❌ Жёсткая привязка через `TELEGRAM_CRON_1` и `TELEGRAM_CRON_2`
- ❌ Использовала `postRandomTopic()` (случайные посты)

### Новая система
- ✅ **4 поста в день** с плавающим временем
- ✅ Случайное время внутри 4 временных окон
- ✅ Строгая последовательность по циклу (1 гуманитарный + 3 технических)
- ✅ Автоматический сброс счётчиков при новом дне

## Что реализовано

### 1. Расширение состояния (`state.js`)

Добавлены поля:
```json
{
  "postsTodayCount": 2,      // Счётчик постов за день
  "dailyPostIndex": 2        // Индекс текущего поста (0-3)
}
```

### 2. Новая функция постинга (`posting.js`)

```javascript
export async function postSingleScheduledTopic()
```

**Логика:**
1. Проверяет, новый ли день → сбрасывает счётчики
2. Проверяет лимит (4 поста в день)
3. Планирует 4 поста на день
4. Отправляет только один пост по индексу `dailyPostIndex`
5. Увеличивает счётчики
6. При 4-м посте обновляет состояние цикла

### 3. Плавающее расписание (`schedule.js`)

**Временные окна:**
```javascript
const POSTING_WINDOWS = [
  { start: 1, end: 5, name: 'Утро' },    // 04:00-08:00 МСК
  { start: 7, end: 11, name: 'День' },   // 10:00-14:00 МСК
  { start: 13, end: 17, name: 'Вечер' }, // 16:00-20:00 МСК
  { start: 19, end: 23, name: 'Ночь' }   // 22:00-02:00 МСК
];
```

**Алгоритм:**
1. При запуске сервера генерируется случайное время для каждого окна
2. Создаётся cron-задача на это время
3. Каждая задача вызывает `postSingleScheduledTopic()`

### 4. CLI команды

Добавлена команда:
```bash
npm run post:single  # Отправить один пост (имитация cron)
```

### 5. Документация

Созданы файлы:
- `FLOATING_SCHEDULE.md` — полная документация
- Обновлён `README.md`

## Тестирование

### Ручной тест

```bash
# Первый пост
npm run post:single
# ✅ Posted 1/4 for today: representation-from-signs-to-formalization

# Второй пост
npm run post:single
# ✅ Posted 2/4 for today: performance-introduction
```

### Проверка состояния

```json
{
  "postsTodayCount": 2,
  "dailyPostIndex": 2,
  "lastPostDate": "2026-01-10",
  "cycleDay": 0,
  "humanitarianIndex": 0
}
```

✅ Счётчики обновляются корректно  
✅ Посты идут по циклу  
✅ Лимит 4 поста в день работает

## Логика работы

### Пример дня

**День 1 (2026-01-10):**

```
03:42 UTC (06:42 МСК) → Пост 1/4: computer-science[0] (гуманитарный)
09:15 UTC (12:15 МСК) → Пост 2/4: optimization[0] (технический)
14:58 UTC (17:58 МСК) → Пост 3/4: security[0] (технический)
21:23 UTC (00:23 МСК) → Пост 4/4: frameworks[0] (технический)
```

После 4-го поста:
- `cycleDay` → 1
- `humanitarianIndex` → 1
- `dailyPostIndex` → 0 (сброс для следующего дня)

**День 2 (2026-01-11):**

```
02:18 UTC (05:18 МСК) → Пост 1/4: hiring[0] (гуманитарный)
08:47 UTC (11:47 МСК) → Пост 2/4: security[1] (технический)
15:33 UTC (18:33 МСК) → Пост 3/4: frameworks[1] (технический)
20:09 UTC (23:09 МСК) → Пост 4/4: architecture[0] (технический)
```

## Преимущества

✅ **Естественность** — посты не в одно и то же время  
✅ **Равномерность** — 4 поста распределены по всем суткам  
✅ **Детерминированность** — последовательность сохраняется  
✅ **Простота** — не нужно настраивать конкретное время  
✅ **Масштабируемость** — легко изменить количество окон

## Включение

Уже включено! В `server/index.js`:

```javascript
setupTelegramCron(); // ← Активно
```

При запуске сервера увидишь:

```
🔧 Setting up floating schedule for Telegram posting...
📊 Configuration: 4 posts per day in random time windows

📅 Scheduling post 1/4 (Утро)
   Time: 03:42 UTC (06:42 МСК)
   Cron: 42 3 * * *

...

✅ Floating schedule initialized successfully!
```

## Файлы проекта

```
server/
├── telegram/
│   ├── schedule.js                    ← Обновлён: плавающее расписание
│   ├── posting.js                     ← Обновлён: +postSingleScheduledTopic()
│   ├── cli.js                         ← Обновлён: +команда single
│   ├── README.md                      ← Обновлён
│   ├── FLOATING_SCHEDULE.md           ← Новый: полная документация
│   └── FLOATING_SCHEDULE_SUMMARY.md   ← Этот файл
├── services/telegram/
│   └── state.js                       ← Обновлён: +postsTodayCount, +dailyPostIndex
├── package.json                       ← Обновлён: +post:single
└── posted-topics.json                 ← Обновлён: новые поля
```

## Команды

```bash
# Ручное тестирование
npm run post:single              # Один пост (имитация cron)
npm run post:schedule            # Все 4 поста сразу
npm run post:preview 7           # Предпросмотр расписания

# Старые команды (работают)
npm run post:random              # Случайный пост
npm run post:topic <id>          # Конкретный пост
```

## Мониторинг

### Проверка состояния
```bash
cat server/posted-topics.json
```

### Проверка журнала
```bash
cat server/posting-log.json
```

### Логи сервера
```
🚀 Cron job 1/4 (Утро) triggered
✅ Post 1/4 successful: {...}
```

## Следующие шаги (опционально)

- [ ] Добавить webhook для уведомлений об ошибках
- [ ] Статистика покрытия разделов
- [ ] Web UI для мониторинга расписания
- [ ] A/B тестирование разных временных окон

## Заключение

Система плавающего расписания полностью реализована, протестирована и готова к использованию.

**Ключевые изменения:**
- 2 поста → 4 поста в день
- Фиксированное время → плавающее время
- Случайные посты → строгая последовательность по циклу

**Готово к продакшену!** 🚀
