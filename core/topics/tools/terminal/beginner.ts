import { Topic } from '../../../types';

export const TERMINAL_BEGINNER_TOPICS: Topic[] = [
  {
    id: 'terminal-basics',
    title: 'Основы Terminal',
    description: 'Terminal (командная строка) — основной инструмент для работы с файловой системой и выполнения команд в Unix-подобных системах. Навигация по директориям: cd для перехода, ls для просмотра содержимого, pwd для текущей директории. Работа с файлами: создание (touch, mkdir), просмотр (cat, less, more), копирование и перемещение (cp, mv), удаление (rm). Поиск файлов и содержимого: find для поиска файлов, grep для поиска текста. Работа с текстом: head/tail для просмотра начала/конца файла, wc для подсчёта строк/слов, sort для сортировки, uniq для уникальных строк, cut/tr для обработки. Права доступа: chmod для изменения прав, chown/chgrp для владельца/группы, umask для маски по умолчанию. История команд: history для просмотра, ! для повторного выполнения, Ctrl+R для поиска в истории.',
    difficulty: 'beginner',
    tags: ['terminal', 'cli', 'bash', 'shell', 'basics', 'tools', 'productivity'],
    keyPoints: [
      'Навигация: cd для перехода в директорию, ls для списка файлов, pwd для текущего пути.',
      'Файлы: touch создаёт файл, mkdir — директорию, cp копирует, mv перемещает/переименовывает, rm удаляет.',
      'Поиск: find ищет файлы по имени/типу, grep ищет текст в файлах.',
      'Текст: head/tail показывают начало/конец файла, wc считает строки/слова, sort сортирует, uniq убирает дубликаты.',
      'Права: chmod изменяет права (rwx), chown меняет владельца, umask задаёт маску по умолчанию.',
      'История: history показывает команды, ! повторяет команду по номеру, !! — последнюю, Ctrl+R — поиск.'
    ],
    examples: [
      {
        title: 'Навигация и просмотр файлов',
        code: `cd /home/user/projects    # Переход в директорию
ls -la                      # Список файлов с деталями
pwd                        # Текущая директория
cat file.txt               # Просмотр файла
less large-file.txt        # Просмотр с прокруткой`
      },
      {
        title: 'Создание и управление файлами',
        code: `touch newfile.txt         # Создать файл
mkdir -p dir1/dir2         # Создать директорию (с родителями)
cp file.txt backup/        # Копировать файл
mv old.txt new.txt         # Переименовать/переместить
rm -rf directory/          # Удалить директорию рекурсивно`
      },
      {
        title: 'Поиск файлов и текста',
        code: `find . -name "*.js"       # Найти все .js файлы
find . -type f -size +100M  # Файлы больше 100MB
grep "error" *.log         # Найти "error" в .log файлах
grep -r "function" src/    # Рекурсивный поиск`
      },
      {
        title: 'Работа с текстом',
        code: `head -n 20 file.txt       # Первые 20 строк
tail -f log.txt            # Последние строки + следить за изменениями
wc -l file.txt             # Количество строк
sort file.txt              # Сортировать строки
sort file.txt | uniq       # Убрать дубликаты
cut -d',' -f1 data.csv     # Первое поле (разделитель запятая)`
      },
      {
        title: 'Права доступа',
        code: `chmod 755 script.sh        # rwxr-xr-x (владелец: все, остальные: чтение+выполнение)
chmod +x script.sh         # Добавить право на выполнение
chown user:group file.txt  # Изменить владельца и группу
umask 022                  # Маска: права по умолчанию 755 для директорий`
      },
      {
        title: 'История команд',
        code: `history                    # Показать историю команд
!42                       # Выполнить команду #42 из истории
!!                        # Повторить последнюю команду
!ls                       # Выполнить последнюю команду, начинающуюся с "ls"
Ctrl+R                    # Интерактивный поиск в истории`
      }
    ],
    relatedTopics: ['terminal-intermediate']
  }
];

